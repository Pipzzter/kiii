# Dev-only placeholder secret so `kubectl apply -k k8s/` works out of the box.
# Replace stringData with real values (or generate via `kubectl create secret`)
# before this ever points at real data.
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: ciid-project
type: Opaque
stringData:
  POSTGRES_PASSWORD: changethis
